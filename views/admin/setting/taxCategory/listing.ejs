
<br/>

              <div class="table-responsive">
                <table class="table table-striped table-md datatable">
                  <thead>
                    <tr>
                      <%
                          if (taxCategoryHeaderColumns.length > 0) {
                            taxCategoryHeaderColumns.forEach((column, index) => {
                        %>
                      <th><%- column; %></th>
                      <%
                            });
                        %>

                      <%
                          }
                        %>
                    </tr>
                  </thead>
                  <tbody>
                    <%
                      if (taxCategoryData.length > 0) {
                        taxCategoryData.forEach(row => {
                    %>
                    <tr>
                      <%
                        if (row.length > 0) {
                          row.forEach(column => {
                      %>
                      <td><%- column; %></td>
                      <%
                          });
                        }
                      %>
                    </tr>
                    <%
                        });
                      }
                    %>
                  </tbody>
                </table>
              </div>
